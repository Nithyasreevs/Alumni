// CommonJS wrapper for the SDK
// Uses the bundled version that includes jose dependency

// Ensure crypto is available for Node.js
if (typeof global !== 'undefined' && !global.crypto) {
  const { webcrypto } = require('crypto');
  global.crypto = webcrypto;
}

const { buildSSORedirectURL, decryptSSOToken } = require('./dist/index.cjs');

module.exports = {
  buildSSORedirectURL,
  decryptSSOToken
}; 